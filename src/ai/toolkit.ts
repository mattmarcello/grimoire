// The AI analysis pipeline uses LanguageModel.generateObject with schemas,
// not tool-based interactions. This module is reserved for future interactive
// tool use with streaming text generation.
//
// The CodebaseReader service handles all file reading directly in the pipeline.

export {}
